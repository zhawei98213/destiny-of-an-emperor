# Reference Frame Extraction Reports
# 参考关键帧提取报告

This directory stores generated frame-pack validation artifacts.
此目录用于存放生成式关键帧包校验产物。

Expected files:
预期文件：

- `latest/report.json`
- `latest/summary.md`

These artifacts are generated by:
这些产物由以下命令生成：

- `npm run reference-frame-extract`

Do not edit files under `latest/` by hand.
不要手工编辑 `latest/` 下的文件。

# Tileset Reconstruction Reports
# Tileset 重建报告

`reports/tileset-reconstruction/latest/` contains the latest structured tileset reconstruction report.
`reports/tileset-reconstruction/latest/` 保存最新的结构化 tileset 重建报告。

The directory is generated and should not be edited by hand.
该目录由工具生成，不应手工编辑。

Expected artifacts:
预期产物：

- `report.json`
- `summary.md`

The normalization task list is written separately to:
归一化任务列表会单独写到：

- `content/generated/import-staging/tileset-crop-plan.generated.json`

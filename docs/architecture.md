# Architecture Notes

The runtime is split into scenes, systems, and content contracts so gameplay logic can migrate toward fully data-driven flows without coupling authoring formats to Phaser scene code.
